import {useContext} from "react";
import {UserContext} from "../context/userContext.jsx";

const Home = () => {

    return (
        <div>
            <h1>Home</h1>
        </div>
    );
};

export default Home;
